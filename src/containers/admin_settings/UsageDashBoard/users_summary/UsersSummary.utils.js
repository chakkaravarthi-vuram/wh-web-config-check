export const topActiveUsersHeaders = (t) => {
  return {
    HEADERS: [
      t('admin_settings.user_management.heading_strings.user_name'),
      t('admin_settings.user_management.invite_member.invite_member_card.email_address_label'),
    ],
  };
};
export const topDevelopersUserseHeaders = (t) => {
  return {
    HEADERS: [
      t('admin_settings.user_management.heading_strings.user_name'),
      t('admin_settings.user_management.invite_member.invite_member_card.email_address_label'),
    ],
  };
};
